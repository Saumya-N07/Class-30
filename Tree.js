class Tree{
    constructor(x,y) {
        this.x=x;
        this.y=y;
        this.height=height;
        this.width=width;
        this.thickness=10;

        this.image=loadImage("images/tree.png")
    }

    display() {
        ImageMode(CENTER);
    }
}